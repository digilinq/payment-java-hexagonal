# Payment Java Hexagonal

This project is a simple example of a hexagonal architecture in Java.

## Build and Run

### Run the application locally

```bash
gradle bootRun --args='--spring.profiles.active=local'
```

